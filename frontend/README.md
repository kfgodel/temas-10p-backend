# Votación de roots (Frontend)

## Correr en `dev`
1. `$ npm install` - Instalar dependencias (`npm` y `bower`)
2. `$ npm start` - Corre un frontend, proxeando el frontend en [localhost:4200](http://localhost:4200)
3. profit
